export type DestinationCard = {
    id: string;
    label: string;
    time: string;
    address: string;
    lat: number;
    lng: number;
};

export type RecentDestination = {
    id: string;
    label: string;
};